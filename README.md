# example-express-app
A minimal Express app that fetches data from a public API introducing JSON objects using Node’s built-in fetch and renders it with EJS. Designed as a simple example of how to structure a clean GitHub repository.
